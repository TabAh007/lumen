# Set this in Vercel (Project → Settings → Environment Variables) to your
# deployed backend's base URL — no trailing /api, no trailing slash.
# Example: https://lumen-backend.onrender.com
VITE_API_URL=
