# Copy to .env and fill in. .env is gitignored.

PORT=5050

# MongoDB Atlas connection string (M0 free tier)
MONGODB_URI=

# Apify (Instagram + TikTok scraping)
APIFY_TOKEN=

# LLM provider for analysis (decide later: anthropic | openai)
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
